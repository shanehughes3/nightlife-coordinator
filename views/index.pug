extends layout.pug

block title
	title Nightlife Coordinator

block content
	form(action="javascript:void(0)")
		input(
			type="text"
			name="term"
			autofocus
			placeholder="Location"
			id="search-term"
		)
		button(
			id="search-button"
			type="submit"
		) Search

	div(id="message")
	div(id="results")